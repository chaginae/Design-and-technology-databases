services:
  db:
    image: postgres:15-alpine
    container_name: django_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mydb}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-123123}
      POSTGRES_APP_PASSWORD: ${POSTGRES_APP_PASSWORD:-django_app_secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} --host=${POSTGRES_HOST:-db} --port=${POSTGRES_PORT:-5432}"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    user: "${UID:-1000}:${GID:-1000}"
    build: .
    container_name: django_web
    environment:
      POSTGRES_USER: django_app
      POSTGRES_PASSWORD: ${POSTGRES_APP_PASSWORD:-django_app_secret}
      POSTGRES_DB: ${POSTGRES_DB:-mydb}
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      DATABASE_URL: postgresql://django_app:${POSTGRES_APP_PASSWORD:-django_app_secret}@db:5432/${POSTGRES_DB:-mydb}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./project8sem:/app/project8sem
    ports:
      - "8000:8000"
    command: >
      sh -c "python manage.py makemigrations &&
             python manage.py migrate &&
             echo '--------------------------------------------------' &&
             echo 'Django development server is running at:' &&
             echo 'http://localhost:8000/' &&
             echo '--------------------------------------------------' &&
             python manage.py runserver 0.0.0.0:8000"

volumes:
  postgres_data: