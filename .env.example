DJANGO_SECRET_KEY="django-insecure--tqbgs69bw5cfgr55_y&tjy0fu#f@&!b99*mxyx=ry3#s=^wze"
DJANGO_ALLOWED_HOSTS="localhost,127.0.0.1,0.0.0.0"

POSTGRES_DB="mydb"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="123123"  # CHANGE IT! (суперпользователь контейнера БД)
# Пароль роли django_app — приложение в Docker подключается под этой ролью (задание 5)
POSTGRES_APP_PASSWORD="django_app_secret"  # задайте свой пароль в .env
# POSTGRES_HOST "db" if starting in compose, "localhost" if database in container and django locally
POSTGRES_HOST="localhost" 
POSTGRES_PORT="5432"